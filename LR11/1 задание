#include <stdio.h>
#include <locale.h>

int main()
{
	setlocale(LC_ALL, "Rus");
	float a, b;
	printf("Введите ва числа\n");
	scanf ("%f  %f", &a, &b);
	if (a == b){
		a=0;
		b=0;
	}
	if(a>b){
		b=a;
	}
	else{
		a=b;
	}
	printf("a = %f, b = %f ", a, b);
	return 0;
}

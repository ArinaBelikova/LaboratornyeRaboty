#include <stdio.h>
#include <locale.h>
 
int main()
{
	setlocale(LC_ALL, "Rus");
	int A, B, C;
	printf("Введите 3 целых числа\n");
	scanf ("%i  %i  %i", &A, &B, &C);
  printf("%d", (A < B) & (B < C));
  return 0;
}

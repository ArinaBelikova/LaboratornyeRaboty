#include <stdio.h>
#include <locale.h>
 
int main()
{
	 setlocale(LC_ALL, "Rus");
	 int A;
	 printf("Введите число\n");
	 scanf ("%i", &A);
   printf("%d\n",(A > 9) & (A < 100) & (A % 2 == 0));
   return 0;
}

#include <stdio.h>
#include <locale.h>
 
int main()
{
   setlocale(LC_ALL, "Rus");
	 int A, B;
	 printf("Введите 2 числа\n");
	 scanf ("%i  %i", &A, &B);
   printf("%d", (A > 2) & (B <= 3));
   return 0;
}

#include<stdio.h>
#include <locale.h>

int main()
{
	setlocale(LC_ALL, "Rus");
	float b;
	printf("Введите размер файла в байтах\n");
	scanf("%f", &b);
	int kg=(int)b/1024;
	printf("Количество полных килобайтов: %i", kg);
	return 0;
}

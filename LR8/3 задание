#include<stdio.h>
#include <locale.h>

int main()
{
	setlocale(LC_ALL, "Rus");
	int A, B;
	printf("Введите 2 целых положительных цисла\n");
	scanf("%i  %i", &A, &B);
	printf("Длина незанятой части отрезка: %i", A%B);
	return 0;
}

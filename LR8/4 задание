#include <stdio.h>
#include <locale.h>
 
int main()
{
	  setlocale(LC_ALL, "Rus");
    int A;
    printf("Введите двузначное число\n");
    scanf ("%i", &A);
    printf("%i\n", (A/10)+(A%10)*10);
    return 0;
}

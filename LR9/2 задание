#include <stdio.h>
#include <locale.h>
 
int main()
{
	  setlocale(LC_ALL, "Rus");
    int K;
    printf("Введите целое число, лежащее в диапазоне 1-365\n");
    scanf ("%i", &K);
    printf("%i\n", K%7);
    return 0;
}

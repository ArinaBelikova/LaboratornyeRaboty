#include <stdio.h>
#include <locale.h>

int main()
{
	setlocale(LC_ALL, "Rus");
	int A, B, k;
	printf("Введите два числа (A > B)\n") ;
	scanf ("%i  %i", &A, &B);
	k = A;
	while (k >= 0){
    k -= B;
	}
	printf("%i\n ",k+B); 
	return 0;
}

#include <stdio.h>
#include <locale.h>

int main()
{
	setlocale(LC_ALL, "Rus");
	int A, B;
	printf("Введите два числа (A < B)\n") ;
	scanf ("%i  %i", &A, &B);
	for  (int i = A; i <= B; i++){
    	for (int j = 1; j <= i; j++) printf("%i ",i);
    	printf("\n");
	}
 
   return 0;
}
